[package]
name = "agent-b-program"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0", features = ["derive"] }

# THIS IS THE ONLY REQUIRED LINE
sp1-zkvm = "2.0"

[lib]
crate-type = ["cdylib"]